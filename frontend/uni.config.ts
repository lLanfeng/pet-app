import { defineConfig } from '@dcloudio/uni-cli';

export default defineConfig({
  // UniApp 项目配置
  platforms: {
    'mp-weixin': {
      // 微信小程序配置
    },
    'app': {
      // 原生 App 配置
    },
    'h5': {
      // H5 配置
    }
  }
});
